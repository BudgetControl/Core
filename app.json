{
  "environments": {
    "test": {
      "scripts": {
        "postdeploy": "touch database/database.sqlite && php artisan migrate --seed",
        "buildpacks": [
          {
            "url": "https://github.com/heroku/heroku-buildpack-php"
          }
        ]
      }
    }
  }
}
